apiVersion: v1
kind: Service
metadata:
  name: {{ include "notesync.fullname" . }}
  labels:
    {{- include "notesync.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "notesync.selectorLabels" . | nindent 4 }}
